# Copiá este archivo a `keystore.properties` y completá con tus datos.
# `keystore.properties` y el .jks están gitignoreados: NUNCA los subas al repo.
#
# Ver FIRMA.md para generar el keystore.
storeFile=satrak-campo.jks
storePassword=CAMBIAR
keyAlias=satrak
keyPassword=CAMBIAR
